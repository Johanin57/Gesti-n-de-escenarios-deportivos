@page
@model Presentacion.Pages.Cmunicipio.IndexModel
@{
    ViewData["Title"] = "Municipio";
}

<H1>Listado de Municipio</H1>

<a asp-page="Crear">Registar Municipio</a>
<table class="table">
    <thead>
    <tr>
        <th> Id </th>
        <th> Nombre </th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model.Municipios)
    {
        <tr>
            <td> @item.Id </td>
            <td> @item.Nombre </td>
            <td>
                <a asp-page="./Editar" asp-route-id="@item.Id">Editar</a>
                <a>  -  </a>
                <a asp-page="./Eliminar" asp-route-id="@item.Id">Eliminar</a>
                <a>  -  </a>
                <a asp-page="./Detalle" asp-route-id="@item.Id">Detalles</a>

            </td>
        </tr>
    }
    </tbody>

</table>